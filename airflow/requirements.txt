# Airflow 2.9.2 requirements for MWAA
# Python 3.11 compatible

# IMPORTANT: MWAA 2.9.2 이미 모든 필요한 패키지를 제공합니다.
# - apache-airflow==2.9.2
# - apache-airflow-providers-amazon==8.25.0
# - boto3 (고정 버전)
# - pandas, numpy 등 일반 라이브러리

# DAG에서 사용하는 모든 패키지가 이미 포함되어 있으므로
# requirements.txt는 비워두거나 추가 패키지만 명시합니다.

# 추가 패키지가 필요한 경우에만 아래에 작성:
# Web crawling libraries
requests==2.31.0
beautifulsoup4==4.12.3
lxml==5.1.0
