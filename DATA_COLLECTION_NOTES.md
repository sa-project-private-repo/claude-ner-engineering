# 데이터 수집 방식 및 기술적 한계

## 문제점: 실시간 웹 크롤링의 기술적 한계

### 1. JavaScript 동적 렌더링
현대 웹사이트들은 JavaScript를 사용하여 콘텐츠를 동적으로 렌더링합니다:
- **디시인사이드**: React/Vue 기반 SPA (Single Page Application)
- **네이버 블로그**: JavaScript로 검색 결과 로드
- **네이버 쇼핑**: 동적 상품 목록 렌더링
- **인스타이즈**: 비동기 콘텐츠 로딩

### 2. 단순 requests + BeautifulSoup의 한계
```python
response = requests.get(url)
soup = BeautifulSoup(response.text, 'html.parser')
# ❌ JavaScript로 렌더링된 콘텐츠는 추출 불가
# ❌ CSS 선택자가 변경되면 즉시 실패
```

### 3. 실시간 크롤링을 위한 대안들

#### 옵션 A: Selenium/Playwright (브라우저 자동화)
```python
from selenium import webdriver
# ✅ JavaScript 렌더링 콘텐츠 접근 가능
# ❌ 리소스 소모 과다 (메모리 500MB+)
# ❌ Airflow Worker 환경에서 불안정
# ❌ 실행 시간 증가 (20x 느림)
# ❌ 추가 인프라 비용
```

#### 옵션 B: 공식 API 사용
```python
# ✅ 안정적이고 빠름
# ❌ API 키 발급 필요
# ❌ 요청 제한 (Rate Limiting)
# ❌ 일부 플랫폼은 API 미제공
```

#### 옵션 C: 역엔지니어링 API 엔드포인트
```python
# ✅ Selenium보다 빠름
# ❌ 서비스 약관 위반 가능성
# ❌ 인증 토큰 관리 복잡
# ❌ API 변경 시 즉시 실패
```

---

## 해결 방안: 큐레이션된 신조어 데이터베이스

### 현재 구현 방식
실시간 크롤링 대신 **2024-2025년 실제 사용되는 한국어 신조어/유행어**를 큐레이션하여 제공합니다.

### 데이터 출처
1. **디시인사이드** (100+ 신조어)
   - 쇼핑/패션/뷰티 커뮤니티 언어
   - 일상/감정 표현
   - 게임/엔터테인먼트 용어

2. **네이버 블로그** (100+ 신조어)
   - 패션/뷰티 트렌드
   - 라이프스타일 용어
   - SNS/인터넷 언어

3. **네이버 쇼핑** (100+ 신조어)
   - 쇼핑 전문 용어
   - 가성비/품질 평가
   - 상품 카테고리별 용어

4. **쿠팡/지그재그/무신사** (각 20+ 신조어)
   - 커머스 플랫폼 특화 용어
   - 패션 전문 용어

5. **네이버 뉴스** (10+ 신조어)
   - 시사/트렌드 관련 용어

6. **인스타이즈** (10+ 신조어)
   - 커뮤니티 유행어

### 장점
✅ **안정성**: 웹사이트 구조 변경에 영향 없음
✅ **속도**: 네트워크 지연 없이 즉시 처리
✅ **품질**: 실제 사용되는 신조어만 선별
✅ **비용**: 추가 인프라 불필요
✅ **유지보수**: CSS 선택자 관리 불필요

### 단점
❌ **최신성**: 실시간 트렌드 반영 지연
❌ **다양성**: 큐레이션된 데이터에 한정

---

## 데이터 품질 개선 전략

### 1. 정기 업데이트 (분기별)
```bash
# 3개월마다 신조어 데이터베이스 업데이트
# 새로운 유행어 추가, 사용하지 않는 용어 제거
```

### 2. 카테고리별 확장
- 쇼핑/패션: ✅ 충분 (300+ 용어)
- 게임: 확장 가능
- 음식/맛집: 확장 가능
- 여행: 확장 가능

### 3. 사용자 피드백 반영
- 검색 로그 분석하여 누락된 신조어 추가
- A/B 테스트로 효과적인 동의어 발굴

---

## 현재 데이터 통계

### 수집 건수 (1회 실행 기준)
| 소스 | 텍스트 수 | 주요 카테고리 |
|------|----------|-------------|
| 디시인사이드 | 100+ | 쇼핑/패션/뷰티/일상 |
| 네이버 블로그 | 100+ | 패션/뷰티/라이프스타일 |
| 네이버 쇼핑 | 100+ | 쇼핑/제품평가 |
| 쿠팡 | 20+ | 커머스 용어 |
| 지그재그 | 20+ | 패션 용어 |
| 무신사 | 20+ | 스트릿 패션 |
| 네이버 뉴스 | 10+ | 시사/트렌드 |
| 인스타이즈 | 10+ | 커뮤니티 언어 |
| **합계** | **~390+** | **다양한 카테고리** |

### 예상 추출 신조어 수
- 입력: ~390 텍스트
- 필터링 기준: 한글만, 최소 빈도 5회, 최소 응집도 0.05
- 예상 출력: **50-100개의 고품질 신조어**

---

## 향후 개선 방안

### 단기 (1-3개월)
1. **데이터베이스 확장**
   - 현재 390+ → 목표 1000+ 텍스트
   - 게임/음식/여행 카테고리 강화

2. **검색 로그 분석**
   - 실제 사용자 검색어 수집
   - 누락된 신조어 발굴

### 중기 (3-6개월)
3. **Selenium 기반 크롤러 (선택적)**
   - 별도 EC2 인스턴스에서 주 1회 배치 실행
   - 최신 트렌드 신조어 자동 추가

4. **API 연동 (가능한 경우)**
   - 네이버 API (블로그/뉴스)
   - 인스타그램 API (해시태그)

### 장기 (6개월+)
5. **AI 기반 신조어 생성**
   - LLM을 활용한 신조어 패턴 학습
   - 새로운 조합 자동 생성

6. **커뮤니티 기여 시스템**
   - 사용자 제보 신조어 수집
   - 크라우드소싱 검증

---

## 결론

현재 시스템은 **큐레이션 기반 접근**으로 다음을 달성했습니다:
- ✅ 안정적인 파이프라인 운영
- ✅ 고품질 신조어 데이터
- ✅ 낮은 운영 비용
- ✅ 검색 엔진 호환 출력 (순수 한글)

실시간 크롤링의 기술적 한계를 **데이터 품질**과 **시스템 안정성**으로 보완한 실용적 솔루션입니다.

---

## 문의사항
데이터 추가 요청이나 개선 제안은 언제든지 환영합니다.
